import 'package:flutter/material.dart';

class RoomDetailsPage extends StatelessWidget {
  static RoomDetailsPage fromRouting(room, avatar, index) => RoomDetailsPage();

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
